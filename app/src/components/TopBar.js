import { boxStyles } from "../utils/const";
import { Box } from "@mui/material";

const TopBar = () => {
    return (
        <>
        <Box
            sx={{
                ...boxStyles
            }}
        >
            
        </Box>
        </>
        
    );
};

export default TopBar;